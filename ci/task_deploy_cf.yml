---
platform: linux
image_resource:
  type: docker-image
  source: {repository: frekele/gradle}
inputs:
- name: git-repo-test
  path: data/
run:
  path: gradle
  args:
    - cfPush
    - {{cfUserName}}
    - {{cfPassword}}]